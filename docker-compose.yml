services:
  frontend:
    build: 
      context: .
      dockerfile: Dockerfile
      tags:
        - "frontend:main"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    ports:
      - "3000:3000"
  redis:
    image: "redis:alpine"
    ports:
      - "6379:6379"
